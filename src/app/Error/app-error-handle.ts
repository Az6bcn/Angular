// /*******************************************************************Global GENERIC Error Handler*********************************************/
// /* To handle Error globally (generic error we don't know), we create this class, it implements 'errorHandler'.
// we Register it in app.module as a replacement for this class */
// import { ErrorHandler } from '@angular/core';
// //ErrorHandler :  search in angular.io
// //To display error to the user and log error to the server

// export class AppErrorHandler implements ErrorHandler {
//     handleError(error){
//         alert('An unexpected error ocurred.');
//         //Log error message on the server
//         //console.log(error);
//     }
// }
