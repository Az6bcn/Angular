import { CustomDirectiveDirective } from './custom-input-format.directive';

describe('CustomDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
